{
  "format": "factory-line-audit/declaration/1",
  "line": "line1",
  "fixture": true,
  "reviewed_by": "fixture",
  "_disclosure": "A FIXTURE. It passes the gate by saying so on its face, never by naming a reviewer. Every number below is invented to give the test corpus a shape; none is a fact about a real machine, and no basis string names a real document. A file like this must never be used to audit a plant.",
  "statements": [
    {"kind": "bound", "asset": "PR-01", "tag": "die_temp_c",
     "warning": 240, "critical": 265,
     "basis": "FIXTURE -- stands in for a die datasheet giving the temper limit"},
    {"kind": "bound", "asset": "PR-01", "tag": "tonnage_kn",
     "warning": 1800, "critical": 2000,
     "basis": "FIXTURE -- stands in for the press frame rating plate"},
    {"kind": "rate", "asset": "PR-01", "tag": "stroke_count",
     "rate_warning": 0.6, "rate_critical": 1.2,
     "basis": "FIXTURE -- stands in for a controls spec giving max strokes per minute"},
    {"kind": "reset", "asset": "PR-01", "tag": "stroke_count",
     "allow_reset": true,
     "basis": "FIXTURE -- stands in for the die-change procedure: the stroke counter is zeroed when a die is changed, so a drop to near zero is the procedure and not a fault"},

    {"kind": "setpoint", "asset": "ST-01", "tag": "cycle_time_s",
     "setpoint": 60, "tolerance": 4,
     "basis": "FIXTURE -- the line takt in the register meta; a designed number, not a learned one"},
    {"kind": "expect_variation", "asset": "ST-01", "tag": "weld_current_a",
     "basis": "FIXTURE -- weld current varies per weld by construction, so a flat trace is a dead probe"},
    {"kind": "reset", "asset": "ST-01", "tag": "parts_out",
     "allow_reset": true,
     "basis": "FIXTURE -- stands in for the shift-report procedure: the station part counters are zeroed at shift handover"},

    {"kind": "setpoint", "asset": "ST-02", "tag": "cycle_time_s",
     "setpoint": 60, "tolerance": 4,
     "basis": "FIXTURE -- the line takt in the register meta"},
    {"kind": "conservation", "asset": "ST-02",
     "input_tag": "parts_in", "output_tags": ["parts_out", "scrap_count"],
     "basis": "FIXTURE -- every part entering the station leaves it as good or as scrap; a claim about the process, which no rule could derive from these names"},
    {"kind": "redundant", "asset": "ST-02", "tag": "parts_out",
     "agrees_with": ["parts_out_mes"], "tolerance": 0.01,
     "basis": "FIXTURE -- the PLC counter and the MES record count the same parts. A claim about the plant: two tags whose names look alike are not evidence they measure one quantity"},
    {"kind": "gate_on", "asset": "ST-02", "tag": "cycle_time_s",
     "required_property": "state_running", "open_when": ["Running"],
     "basis": "FIXTURE -- cycle time means nothing during a planned stop or a changeover. open_when stands in for the station state machine document: the word that means running. Without it the gate can only read a boolean, and every other word a PLC produces would open it"},
    {"kind": "reset", "asset": "ST-02", "tag": "parts_in",
     "allow_reset": true,
     "basis": "FIXTURE -- stands in for the shift-report procedure: zeroed at shift handover with the rest of the station counters"},
    {"kind": "reset", "asset": "ST-02", "tag": "parts_out",
     "allow_reset": true,
     "basis": "FIXTURE -- stands in for the shift-report procedure: zeroed at shift handover"},
    {"kind": "reset", "asset": "ST-02", "tag": "scrap_count",
     "allow_reset": true,
     "basis": "FIXTURE -- stands in for the shift-report procedure: zeroed at shift handover"},
    {"kind": "reset", "asset": "ST-02", "tag": "parts_out_mes",
     "allow_reset": false,
     "basis": "FIXTURE -- stands in for the MES record retention note, and the one answer here that differs from its neighbours: the MES count is cumulative for the order and is never zeroed, so a drop to near zero is the record going backwards. Declared false so the engine names it a reversal rather than a reset of a counter that has none"},

    {"kind": "setpoint", "asset": "ST-03", "tag": "cycle_time_s",
     "setpoint": 60, "tolerance": 4,
     "basis": "FIXTURE -- the line takt in the register meta"},
    {"kind": "reset", "asset": "ST-03", "tag": "parts_out",
     "allow_reset": true,
     "basis": "FIXTURE -- stands in for the shift-report procedure: zeroed at shift handover"},

    {"kind": "bound", "asset": "ROB-01", "tag": "program_cycle_ms",
     "warning": 800, "critical": 1200,
     "basis": "FIXTURE -- stands in for a robot controller cycle-time limit"},
    {"kind": "bound", "asset": "ROB-01", "tag": "axis1_temp_c",
     "warning": 65, "critical": 80,
     "basis": "FIXTURE -- stands in for a servo motor thermal limit"},

    {"kind": "setpoint", "asset": "CNV-01", "tag": "speed_mpm",
     "setpoint": 12, "tolerance": 1.2,
     "basis": "FIXTURE -- stands in for the commissioned line speed"},
    {"kind": "slow_oscillation", "asset": "CNV-01", "tag": "speed_mpm",
     "min_amplitude": 0.10, "min_crossings": 4,
     "basis": "FIXTURE -- drive hunting on this conveyor has a period of minutes, which the default period-2 arm cannot see (probe S2)"},
    {"kind": "bound", "asset": "CNV-01", "tag": "motor_current_a",
     "warning": 18, "critical": 24,
     "basis": "FIXTURE -- stands in for a motor nameplate current"},

    {"kind": "rate", "asset": "PLC-01", "tag": "heartbeat",
     "rate_warning": 60, "rate_critical": 120,
     "basis": "FIXTURE -- the heartbeat word increments once per 20 ms scan, so 50/s nominal. Declared because the engine otherwise answers this arm from a default of 0.1/s warning and 0.5/s critical (probe R1), which every PLC heartbeat crosses"},
    {"kind": "reset", "asset": "PLC-01", "tag": "heartbeat",
     "allow_reset": true,
     "basis": "FIXTURE -- stands in for the controller data-type note: the heartbeat word wraps at its DINT maximum, and restarts from zero when the controller restarts"},
    {"kind": "bound", "asset": "PLC-01", "tag": "scan_time_ms",
     "warning": 20, "critical": 40,
     "basis": "FIXTURE -- stands in for the configured scan budget"},
    {"kind": "bound", "asset": "PLC-01", "tag": "cpu_load_pct",
     "warning": 70, "critical": 85,
     "basis": "FIXTURE -- stands in for a controller vendor guidance figure"},

    {"kind": "exclusion", "asset": "MES-GW",
     "reason": "Not process equipment. The gateway is IT infrastructure with its own monitoring, and auditing it here would report an IT fault as a line fault",
     "basis": "FIXTURE -- stands in for a scope decision recorded with the plant"}
  ]
}
